# Create Your First Graphic

In this guide you'll learn how to set up a new graphics project from scratch in React. 
We'll then create a Lowerthird that receives and displays dynamic text, and animates 
in/out when receiving `play`/`stop` commands. Here's what we'll be creating:

<video src="/in-out-animations.mp4" autoPlay controls loop style={{ margin: '20px 0', borderRadius: 4 }} />
